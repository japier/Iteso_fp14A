#include <stdio.h>
typedef struct ESPADA{
	int fuerza;
}ESPADA;
typedef struct JUGADOR{
	int tipo;
	char * nombre;
	ESPADA * espada;
}JUGADOR;
void letrasRepetidas(char * palabra){
	//"a n u"
}
int main(){

	JUGADOR alumno;
	ESPADA * espada = malloc(sizeof(espada)*4);
	printf("%d", sizeof(espada));

	funcion("JualnLuna");
	return 0;
}